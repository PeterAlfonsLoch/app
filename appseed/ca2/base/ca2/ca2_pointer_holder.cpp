#include "framework.h"

/*
raw_pointer::raw_pointer(sp(base_application) papp) :
   element(papp)
{

   m_pca = NULL;

}

raw_pointer::raw_pointer(const raw_pointer & holder) :
   element(holder.get_app())
{

   m_pca = holder.m_pca;

}

raw_pointer::raw_pointer(sp(element) pca) :
   element(pca != NULL ? pca->get_app() : NULL)
{

   m_pca = pca;

}
*/
