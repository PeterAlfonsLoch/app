#pragma once

namespace winservice_filesystemsize
{

   /*class CLASS_DECL_CA2_TESSERACT service : 
      public service_base
   {
   public:
      service(::ca::application * papp);
      virtual ~service();

      virtual void Start(DWORD);
      virtual void Stop(DWORD);
      void ServiceThread();

      string stage();

      bool  m_bTimerOn;
      int   m_iFileSizeViewId;

      HANDLE m_hmapRequest;
      HANDLE m_hmapResponse;

      manual_reset_event m_stopped;
      bool m_stopping;

   };*/

} // namespace winservice_filesystemsize