#pragma once


int32_t spaboot_start(const char * pszVersion, const char * pszId);

/*BSTR BSTRFromCStr_dup(UINT codePage, LPCSTR s);

bool TextOutU_dup(HDC hdc, int32_t x, int32_t y, const char * pszUtf8, int32_t iSize);

void FillSolidRect_dup(HDC hdc, LPCRECT lpRect, COLORREF clr);

HFONT CreatePointFontIndirect_dup(const LOGFONTW* lpLogFont, HDC hdcParam);

HFONT CreatePointFont_dup(int32_t nPointSize, const char * lpszFaceName, HDC hdc);

// pLogFont->nHeight is interpreted as PointSize * 10
HFONT CreatePointFontIndirect_dup(const LOGFONTW* lpLogFont, HDC hdcParam);
*/