#pragma once

// STRICT is the only supported option (NOSTRICT is no longer supported)
#ifndef STRICT
#define STRICT 1
#endif


#include <stdlib.h>
#include <stdio.h>

#ifndef EXPORT
#define EXPORT
#endif

